"use client";

// ParticleOverlay o'chirildi — performance bottleneck edi
// requestAnimationFrame loop doimo ishlardi, GPU ni band qilardi
export default function ParticleOverlay() {
    return null;
}

